machine:
  node:
    version: 6.9.4

dependencies:
  pre:
    - curl -o- -L https://yarnpkg.com/install.sh | bash
  cache_directories:
    - ~/.yarn-cache
  override:
    - yarn

test:
  override:
    - yarn lint
    - yarn test
    - yarn e2e
